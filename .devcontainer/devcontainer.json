{
    "name": "Project Development",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    
    // Install PostgreSQL and related tools
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-contrib postgresql-client",
    
    // Optional: Configure PostgreSQL to start automatically
    "postStartCommand": "sudo service postgresql start",
    
    "remoteUser": "vscode"
}
